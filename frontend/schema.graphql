schema {
  query: RootQueryType
}

type RootQueryType {
  ferrets(foster: Boolean): [Ferret!]!
}

type Ferret {
  id: ID!
  ageMonths: Int!
  ageYears: Int!
  available: Boolean!
  bio: String
  fee: String!
  foster: Boolean!
  gender: String!
  imageUrl: String
  name: String!
}
